import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  search: {
    marginBottom: 25,
    marginTop: 10,
  },
  txt: {
    color: "#fff",
  },
  pagination: {
    margin: "auto",
    marginTop: 20,
    marginBottom: 15,
  },
}));
export default useStyles;
