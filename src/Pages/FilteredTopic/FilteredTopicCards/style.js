import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  pagination: {
    margin: "auto",
    position: "absolute",
    bottom: "1px",
  },
});

export default useStyles;
