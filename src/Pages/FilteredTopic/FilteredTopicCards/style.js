import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  pagination: {
    margin: "auto",
    position: "absolute",
    bottom: "1px",
  },
  btn: {
    marginTop: "20",
  },
});

export default useStyles;
