import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  btnstyle: {
    margin: "5px 0",
    color: "#000",
    background: "#EDF6F9",
    borderRadius: "8px",
  },
}));

export default useStyles;
