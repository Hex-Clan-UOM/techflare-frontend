import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  root: {
    background: "#2D4059",
  },
  txt: {
    color: "#fff",
  },
}));
export default useStyles;
