import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  btnStyle: {
    margin: "5px 0",
    color: "#000",
    background: "#EDF6F9",
    borderRadius: "10px",
    marginRight: 20,
  },
  txt: {
    padding: 10,
  },
});
