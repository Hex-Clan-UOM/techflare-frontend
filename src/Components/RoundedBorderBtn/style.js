import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  root: {
    margin: "5px 0",
    color: "#000",
    background: "#EDF6F9",
    borderRadius: "8px",
    marginLeft: 20,
  },
  txt: {
    padding: 10,
  },
}));

export default useStyles;
